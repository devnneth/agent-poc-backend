version: '3.8'

services:
  api:
    build: .
    image: agent-poc-project-prod:latest
    container_name: agent-poc-project-prod
    ports:
      - "8000:8000"
    env_file:
      - .env.prod
    environment:
      - PORT=8000
    restart: unless-stopped
